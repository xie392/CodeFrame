## 1. 基础画布架构
- [x] 1.1 安装依赖 `@use-gesture/react`
- [x] 1.2 创建画布状态管理逻辑（内联在 App.tsx 中）
- [x] 1.3 实现画布容器（Transform Layer + CSS transform）
- [x] 1.4 实现画布背景（淡灰底色 + 可选 padding 彩色区域）

## 2. 画布交互功能
- [x] 2.1 实现鼠标滚轮缩放（以鼠标位置为中心点，passive:false）
- [x] 2.2 实现空格 + 拖拽平移画布（地图模式）
- [x] 2.3 实现画布控制条 UI（缩放按钮、滑块、百分比显示、重置按钮）
- [x] 2.4 添加缩放边界限制（min: 25%, max: 400%）
- [x] 2.5 使用原生 CSS transform（遵循 KISS 原则）

## 3. 代码窗口改造
- [x] 3.1 创建代码窗口组件（内联在 App.tsx 中，支持画布中定位）
- [x] 3.2 实现代码窗口标题栏拖拽移动功能
- [x] 3.3 实现单击进入编辑模式（原生 textarea）
- [x] 3.4 实现编辑模式退出（失焦/Esc 保存）
- [x] 3.5 编辑模式下禁用画布手势，Space+拖拽时禁用窗口交互

## 4. 左侧面板简化
- [x] 4.1 移除 CodeMirror 编辑器区域
- [x] 4.2 将左侧面板宽度从 360px 调整为 240px
- [x] 4.3 简化面板布局，仅保留 padding、主题、背景、导出

## 5. 样式与视觉优化
- [x] 5.1 背景色仅围绕代码窗口（padding 区域），画布底色为淡灰
- [x] 5.2 代码窗口阴影仅在 padding=0 时显示
- [x] 5.3 画布光标状态变化（Space 按下 → grab）
- [x] 5.4 主题切换时窗口样式同步更新（含 textColor）

## 6. 代码窗口尺寸调整
- [x] 6.1 实现 resize handle（右下角拖拽）
- [x] 6.2 尺寸范围限制（320×200 ~ 1200×800）

## 7. 缩放控制优化
- [x] 7.1 缩放按钮/滑块以画布视口中心为锚点缩放
- [x] 7.2 抽取 zoomAt 函数统一缩放逻辑

## 8. 编辑器简化
- [x] 8.1 用原生 textarea 替代 CodeMirror（无行号、更轻量）
- [x] 8.2 支持 Tab 键缩进
- [x] 8.3 光标颜色、文字颜色随主题变化
- [x] 8.4 为每个主题添加 textColor 属性
