# cicd 规范

## 目的
待定 - 由归档变更 add-cicd-workflow 创建。归档后请更新目的。
## 需求
### 需求：标签触发构建

当推送符合语义化版本格式的标签时，系统**必须**自动触发构建流水线。

#### 场景：推送有效标签触发构建

- **当** 推送格式为 `v*.*.*` 的标签（如 `v1.0.0`）
- **那么** 自动触发构建工作流
- **并且** 使用标签版本号作为发布版本

#### 场景：推送无效标签不触发构建

- **当** 推送格式不符合 `v*.*.*` 的标签
- **那么** 不触发构建工作流

---

### 需求：自动构建扩展

系统**必须**在触发后自动构建 Chrome 扩展。

#### 场景：成功构建扩展

- **当** 构建工作流被触发
- **那么** 检出代码
- **并且** 安装项目依赖（使用 pnpm）
- **并且** 执行 `pnpm build` 构建扩展
- **并且** 构建产物输出到 `dist` 目录

#### 场景：构建失败中止流程

- **当** 构建过程发生错误
- **那么** 中止工作流
- **并且** 不创建 Release

---

### 需求：自动打包压缩

构建成功后，系统**必须**将构建产物打包为 zip 格式。

#### 场景：成功打包扩展

- **当** 构建成功完成
- **那么** 将 `dist` 目录内容打包为 zip 文件
- **并且** 文件命名为 `codeframe-{version}.zip`
- **并且** 不包含源码映射文件（.map）

---

### 需求：自动发布 Release

系统**必须**在打包完成后自动创建 GitHub Release。

#### 场景：成功创建 Release

- **当** 打包成功完成
- **那么** 创建 GitHub Release
- **并且** 使用标签名作为 Release 标题
- **并且** 将 zip 文件附加为发布产物

#### 场景：Release 已存在

- **当** 对应版本的 Release 已存在
- **那么** 更新现有 Release 的产物

---

### 需求：Issue 模板

系统**必须**提供标准化的 Issue 模板，帮助用户有效报告问题或请求功能。

#### 场景：提交 Bug 报告

- **当** 用户选择 Bug Report 模板
- **那么** 显示结构化表单
- **并且** 表单包含：问题描述、复现步骤、期望行为、实际行为、环境信息

#### 场景：提交功能请求

- **当** 用户选择 Feature Request 模板
- **那么** 显示结构化表单
- **并且** 表单包含：功能描述、使用场景、期望解决方案

---

### 需求：PR 模板

系统**必须**提供标准化的 PR 模板，确保代码变更的可审查性。

#### 场景：创建 Pull Request

- **当** 用户创建新的 Pull Request
- **那么** 自动填充 PR 模板
- **并且** 模板包含：变更描述、变更类型、测试清单、截图对比（可选）

