## 修改需求

### 需求:箭头渲染样式
箭头必须使用开放 V 形样式（`——>`），禁止使用实心三角形+直线样式。Leafer Arrow 元素的 `endArrow` 属性必须设置为 `'arrow'`。

#### 场景:绘制新箭头
- **当** 用户使用箭头工具在画布上拖拽绘制
- **那么** 箭头末端显示为开放 V 形标记，无填充三角形

#### 场景:旧数据加载
- **当** 加载已有箭头数据（`style: 'single'`）
- **那么** 箭头仍渲染为开放 V 形，`endArrow` 使用 `'arrow'`
