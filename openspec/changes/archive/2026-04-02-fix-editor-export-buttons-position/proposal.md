# 变更：Editor 导出按钮固定底部显示

## 为什么

Editor 右侧属性面板的「导出图片」和「复制到剪贴板」按钮会随着上方内容滚动，用户有时需要滚动到最底部才能看到这些操作按钮，这不符合常用操作应始终可见的 UX 原则。

## 变更内容

- 将右侧属性面板的导出和复制按钮固定在面板底部
- 使上方内容区域可独立滚动，底部操作按钮始终可见

## 影响

- 受影响规范：`editor`
- 受影响代码：`src/editor/App.tsx` 中的 `PropertiesPanel` 组件
