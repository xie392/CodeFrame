## 1. 布局改造
- [x] 1.1 将 Editor 中央画布从 flex 居中改为 relative 定位 + transform 缩放模式（参考 CodeGen 的 PreviewArea 结构）
- [x] 1.2 为 CanvasImage 组件添加默认尺寸约束（最大宽度 800px、最大高度 600px），使图片在画布中呈中等大小居中显示

## 2. 缩放功能
- [x] 2.1 添加缩放状态管理（scale、offset）及 `zoomAt` 锚点缩放函数
- [x] 2.2 实现鼠标滚轮缩放（wheel 事件监听，放大系数 1.08，缩小系数 0.92，范围 0.25x–4x）
- [x] 2.3 实现画布拖拽平移（支持缩放后拖动查看图片区域）

## 3. 缩放控制 UI
- [x] 3.1 在画布底部中央添加浮动缩放控制条（缩小按钮 + 百分比显示 + 滑块 + 放大按钮 + 重置按钮）

## 4. 验证
- [x] 4.1 验证不同尺寸图片在默认视图下的居中显示效果
- [x] 4.2 验证缩放、平移交互流畅性
- [x] 4.3 验证标注工具在缩放后仍然正常工作（坐标映射正确）— 当前标注功能为占位状态，拖拽平移通过 activeTool 条件判断可预留兼容
