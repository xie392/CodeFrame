## 1. 实施

- [x] 1.1 修改 `DEFAULT_FRAME_SETTINGS.background` 配置
  - 将 `type` 从 `'solid'` 改为 `'linear'`
  - 将 `gradientColors` 改为 `['#E0F7FA', '#E1BEE7']`
  - 保持 `gradientAngle: 135` 不变
- [x] 1.2 验证默认背景显示效果
  - 打开 Editor 加载任意图片
  - 确认容器背景显示为浅青蓝到淡紫的渐变
- [x] 1.3 验证背景预设选择功能
  - 确认仍可切换到其他预设（透明、白色、黑色等）
  - 确认自定义背景颜色功能正常
