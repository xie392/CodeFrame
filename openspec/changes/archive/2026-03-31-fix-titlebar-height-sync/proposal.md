# 变更：修复 title_bar 切换后代码窗口高度不同步

## 为什么

当用户取消 title_bar 后点击代码编辑器触发自适应高度，再开启 title_bar 时，代码窗口高度不会自动更新，导致底部两行代码被截断（被"吞掉"），需要再次点击编辑器才能恢复。

## 变更内容

- 修复 `showHeader` 切换时非编辑状态下窗口高度不更新的问题

## 影响

- 受影响规范：`codegen`
- 受影响代码：`src/codegen/App.tsx:512-520`（自适应高度 useEffect）
