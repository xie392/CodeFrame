## 新增需求

### 需求:选择工具框选
选择工具下，用户必须能在空白区域拖拽绘制矩形选取框，选取框内的所有图形必须被选中。

#### 场景:拖拽框选
- **当** 用户在选择工具下于空白区域按下鼠标并拖拽
- **那么** 显示蓝色虚线选取框，框内图形被选中

#### 场景:框选包含多种类型图形
- **当** 选取框内同时包含箭头、矩形、文字、马赛克
- **那么** 所有类型图形均被选中，属性面板显示最后选中的图形属性

#### 场景:点击空白区域取消选中
- **当** 用户在选择工具下点击空白区域（未拖拽）
- **那么** 清除所有选中状态
