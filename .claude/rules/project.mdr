---
description: 
globs: 
alwaysApply: false
---

# CodeFrame 项目开发规范

> 此规则为 CodeFrame 项目的强制开发规范，所有 AI 助手在参与本项目建设时必须严格遵守。

---

## 一、项目概述

**CodeFrame** 是一款 Chrome 浏览器扩展插件，核心功能：截图捕获 + 图片标注 + 代码美化。

**技术栈**: React 19 + TypeScript + Vite + Zustand + TailwindCSS + Shiki + LeaferJS

---

## 二、架构设计模式（强制）

### 2.1 模块化架构

项目采用**功能模块化架构**，每个模块独立自治：

```
src/
├── background/     # Service Worker - 消息路由、存储管理
├── popup/          # 主弹窗入口
├── editor/         # 图片编辑器模块
├── codegen/        # 代码生成器模块
├── content/        # 内容脚本 - 截图捕获
├── shared/         # 共享模块 - 组件、工具、状态
└── options/        # 设置页面
```

**约束**：
- 禁止跨模块直接导入内部实现
- 模块间通信必须通过 `shared/` 或消息传递
- 新增功能必须判断归属模块，不可随意放置

### 2.2 模块内部结构（强制）

每个功能模块必须遵循统一结构：

```
模块目录/
├── components/           # UI 组件（PascalCase 目录）
│   └── ComponentName/
│       └── index.tsx
├── hooks/               # 自定义 Hooks（单一职责）
│   ├── index.ts        # 导出入口
│   └── useHookName.ts
├── store/               # Zustand 状态管理
│   └── module-store.ts
├── utils/               # 工具函数
├── types.ts             # 类型定义
├── constants.ts         # 常量定义
└── App.tsx              # 模块主组件
```

### 2.3 状态管理模式

**使用 Zustand 进行集中式状态管理**：

```typescript
// ✅ 正确：集中式 Store 设计
export const useEditorStore = create<EditorStore & EditorActions>((set, get) => ({
  // 状态分组
  theme: initialThemeState,
  window: initialWindowState,
  // ...
  
  // 操作方法
  setTheme: (theme) => set({ theme }),
}));

// ✅ 正确：函数式更新支持
type SetStateAction<T> = T | ((prev: T) => T);

// ❌ 禁止：分散的状态管理
const [state, setState] = useState({}); // 复杂状态禁止使用 useState
```

**状态分组原则**：
- 按功能域分组（theme, window, editor, canvas）
- 状态和操作方法放在一起
- 持久化使用 `chrome.storage.local` 适配器

### 2.4 自定义 Hook 模式

每个 Hook 必须遵循**单一职责原则**：

| 命名 | 职责 | 示例 |
|------|------|------|
| `use[Feature]` | 功能逻辑封装 | `useZoomPan`, `useExport` |
| `use[Feature]Ref` | Ref 同步 | `useSyncedRef` |
| `use[Feature]History` | 历史记录 | `useEditorHistory` |

```typescript
// ✅ 正确：单一职责，明确输入输出
function useZoomPan(canvasRef: RefObject<HTMLDivElement>) {
  const [zoom, setZoom] = useState(1);
  const [pan, setPan] = useState({ x: 0, y: 0 });
  
  // 逻辑实现...
  
  return { zoom, pan, setZoom, setPan, resetView };
}

// ❌ 禁止：多功能混杂
function useCanvas() {
  // 包含缩放、导出、裁剪、历史... ← 违反单一职责
}
```

---

## 三、代码规范（强制）

### 3.1 命名约定

```yaml
变量/函数: camelCase        # getUserById, userName
常量: UPPER_SNAKE_CASE     # MAX_RETRY_COUNT, API_BASE_URL
组件/类型/接口: PascalCase  # UserService, UserProfile
文件名: kebab-case          # user-profile-card.tsx
目录名: PascalCase          # ComponentName/
私有成员: _前缀             # _internalState
Hook: use前缀               # useZoomPan
Store: -store后缀           # editor-store.ts
```

### 3.2 TypeScript 约束

```typescript
// ✅ 强制：严格模式
"strict": true

// ❌ 禁止：any 类型
const data: any = {}; // ← 禁止

// ✅ 推荐：接口优于类型别名
interface UserProps {
  id: string;
  name: string;
}

// ✅ 推荐：类型守卫
function isUser(obj: unknown): obj is User {
  return typeof obj === 'object' && obj !== null && 'id' in obj;
}
```

### 3.3 函数设计原则

```typescript
// ✅ 推荐：早返回，避免嵌套
async function fetchUser(id: string) {
  if (!id) return null;
  const user = await getUser(id);
  if (!user) return null;
  if (!user.active) return null;
  return user;
}

// ✅ 推荐：纯函数优先
function calculateTotal(items: Item[]): number {
  return items.reduce((sum, item) => sum + item.price, 0);
}

// ❌ 避免：过度嵌套
async function badExample(id: string) {
  if (id) {
    const user = await getUser(id);
    if (user) {
      if (user.active) { /* ... */ }
    }
  }
}
```

**约束**：
- 单行长度 ≤ 80 字符
- 圈复杂度 ≤ 5
- 函数长度 ≤ 50 行（超过需拆分）

### 3.4 组件设计规范

```tsx
// ✅ 正确：组件结构
interface ButtonProps {
  variant?: 'primary' | 'secondary';
  children: React.ReactNode;
  onClick?: () => void;
}

export function Button({ variant = 'primary', children, onClick }: ButtonProps) {
  return (
    <button
      className={cn(
        'base-styles',
        variant === 'primary' && 'primary-styles',
        variant === 'secondary' && 'secondary-styles'
      )}
      onClick={onClick}
    >
      {children}
    </button>
  );
}

// ✅ 正确：使用 cn() 合并样式
import { cn } from '@/shared/lib/utils';
```

---

## 四、Chrome 扩展约束（强制）

### 4.1 消息通信规范

```typescript
// ✅ 正确：类型安全的消息定义
// shared/messages.ts
export type MessageType =
  | 'CAPTURE_REQUEST'
  | 'CAPTURE_RESULT'
  | 'EDIT_IMAGE'
  | 'GENERATE_CODE';

export interface Message<T = unknown> {
  type: MessageType;
  payload?: T;
}

// ✅ 正确：消息验证
function validateMessage(message: unknown, sender: chrome.runtime.MessageSender) {
  // 验证来源：确保消息来自同一扩展
  if (!sender.id || sender.id !== chrome.runtime.id) {
    return null;
  }
  // 验证消息结构...
}
```

### 4.2 存储适配器模式

```typescript
// ✅ 正确：环境适配
const createStorageAdapter = () => {
  if (isChromeExtension) {
    // Chrome Extension 环境
    return {
      getItem: (key) => chrome.storage.local.get(key),
      setItem: (key, value) => chrome.storage.local.set({ [key]: value }),
      removeItem: (key) => chrome.storage.local.remove(key),
    };
  } else {
    // Web 环境
    return {
      getItem: (key) => localStorage.getItem(key),
      setItem: (key, value) => localStorage.setItem(key, value),
      removeItem: (key) => localStorage.removeItem(key),
    };
  }
};
```

### 4.3 Manifest V3 约束

```yaml
禁止:
  - eval() 执行
  - 内联脚本
  - 远程代码加载
  - 跨域请求（需声明 permissions）

存储限制:
  chrome.storage.local: ≤ 10MB
  IndexedDB: 无限制（用于截图历史、缓存）
```

---

## 五、测试规范

### 5.1 测试策略

| 测试类型 | 工具 | 覆盖率要求 | 位置 |
|----------|------|------------|------|
| 单元测试 | Vitest | ≥ 60% | `模块/__tests__/*.test.ts` |
| 集成测试 | Vitest | - | `__tests__/integration/` |
| E2E 测试 | Playwright | - | `e2e/*.spec.ts` |

### 5.2 测试文件组织

```
src/
├── editor/
│   ├── __tests__/
│   │   ├── store.test.ts
│   │   └── utils.test.ts
│   └── hooks/__tests__/
│       └── useZoomPan.test.ts
└── shared/
    └── stores/__tests__/
        └── settings-store.test.ts
```

### 5.3 测试命名规范

```typescript
// ✅ 正确：describe/it 结构
describe('useZoomPan', () => {
  describe('zoom functionality', () => {
    it('should initialize with zoom level 1', () => {
      // ...
    });

    it('should clamp zoom between 0.1 and 5', () => {
      // ...
    });
  });
});
```

---

## 六、OpenSpec 工作流（强制）

### 6.1 变更提案流程

```
新功能/重大变更请求？
├─ 是 → 创建 OpenSpec 提案
│   ├── 1. 运行 `openspec-cn list --specs` 了解现有功能
│   ├── 2. 创建 `openspec/changes/<change-id>/`
│   ├── 3. 编写 proposal.md、tasks.md、spec.md
│   ├── 4. 运行 `openspec-cn validate <id> --strict`
│   └── 5. 等待批准后实施
│
└─ 否（bug修复/拼写错误/配置）→ 直接修复
```

### 6.2 变更ID命名

```yaml
格式: 动词-名词（kebab-case）
前缀:
  add-: 新增功能
  update-: 更新功能
  remove-: 移除功能
  refactor-: 重构

示例:
  add-dark-theme-support
  update-editor-toolbar
  remove-deprecated-api
  refactor-codegen-module
```

### 6.3 规范增量格式

```markdown
## 新增需求
### 需求：功能名称
系统必须提供...

#### 场景：成功场景
- **当** 用户执行操作
- **那么** 预期结果

## 修改需求
### 需求：现有功能
[完整修改后的需求内容]

## 移除需求
### 需求：旧功能
**原因**：[移除原因]
**迁移**：[迁移方案]
```

---

## 七、设计系统规范

### 7.1 色彩系统

```yaml
背景:
  primary: "#0A0A0A"
  secondary: "#0F0F0F"
  tertiary: "#1F1F1F"

文字:
  primary: "#FAFAFA"
  secondary: "#6B7280"
  tertiary: "#4B5563"

强调色:
  primary: "#10B981"    # Emerald 绿 - 主色调
  warning: "#F59E0B"
  info: "#06B6D4"
  error: "#EF4444"

边框:
  primary: "#2a2a2a"
```

### 7.2 字体系统

```yaml
主字体: JetBrains Mono
  - 导航、标题、按钮、标签

辅字体: IBM Plex Mono
  - 描述、正文、副标题

字体大小:
  xs: 11px   - 辅助文字
  sm: 12px   - 按钮、标签
  base: 13px - 正文、导航
  lg: 14px   - 小标题
  xl: 16px   - 标题
  2xl: 20px  - Logo
  3xl: 28px  - 页面标题
```

### 7.3 视觉特征

- **零圆角** (0px)
- **无阴影**
- **终端语法 UI 元素** (>, $, //, [])

---

## 八、Git 工作流

### 8.1 分支策略

```yaml
main: 生产分支，受保护
dev: 开发分支
feature/*: 功能分支
fix/*: 修复分支
refactor/*: 重构分支
```

### 8.2 提交规范

```yaml
feat: 新功能
fix: Bug 修复
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试
chore: 构建/工具
```

---

## 九、安全检查清单

- [ ] 无硬编码密钥、密码、内网 IP
- [ ] 无动态拼接 SQL/Shell/URL
- [ ] 无反序列化不可信数据
- [ ] 第三方库版本已扫漏洞
- [ ] 消息来源验证（sender.id === chrome.runtime.id）
- [ ] 存储数据不包含敏感信息

---

## 十、开发流程检查清单

### 任务开始前

- [ ] 阅读 `openspec/project.md` 了解项目约定
- [ ] 阅读 `openspec/AGENTS.md` 了解工作流程
- [ ] 运行 `openspec-cn list` 查看活动变更
- [ ] 运行 `openspec-cn list --specs` 查看现有功能
- [ ] 检查相关规范 `openspec/specs/<capability>/spec.md`

### 新功能开发

- [ ] 创建 OpenSpec 提案
- [ ] 等待提案批准
- [ ] 遵循模块化架构放置代码
- [ ] 编写单元测试（覆盖率 ≥ 60%）
- [ ] 更新文档和 CHANGELOG

### 代码提交前

- [ ] 运行 `pnpm lint` 检查代码规范
- [ ] 运行 `pnpm test` 确保测试通过
- [ ] 运行 `pnpm build` 确保构建成功
- [ ] 检查 TypeScript 类型错误

---

## 附录：常用命令

```bash
# OpenSpec
openspec-cn list                    # 列出活动变更
openspec-cn list --specs            # 列出规范
openspec-cn validate <id> --strict  # 验证变更

# 开发
pnpm dev                            # 启动开发服务器
pnpm build                          # 构建生产版本
pnpm test                           # 运行测试
pnpm lint                           # 代码检查

# 测试覆盖率
pnpm test -- --coverage             # 生成覆盖率报告
```

---

*此规则为项目强制规范，违反即视为任务失败。*
