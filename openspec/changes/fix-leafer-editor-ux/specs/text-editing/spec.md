## 修改需求

### 需求:文字编辑定位
文字编辑时 TextEditorInput 的位置必须与 Leafer 内文字元素的位置完全对齐，禁止出现整体画面偏移。编辑过程中 Leafer 画布不得发生位移或缩放变化。

#### 场景:双击编辑文字
- **当** 用户双击文字进入编辑模式
- **那么** TextEditorInput 精确覆盖在文字元素位置上，整体画面无偏移

#### 场景:编辑中缩放
- **当** 文字处于编辑状态且用户缩放视口
- **那么** TextEditorInput 跟随缩放更新位置和字号
